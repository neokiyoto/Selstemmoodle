
  # Multi-Module Platform Design

  This is a code bundle for Multi-Module Platform Design. The original project is available at https://www.figma.com/design/iufrr37MQBNObB8OqwGuUN/Multi-Module-Platform-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  